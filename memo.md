![alt text](image.png)

Phase 1: 개발 환경 세팅과 Git의 첫인상 (기초 공사)
코드를 짜기 전에, 내 코드를 안전하게 보관할 '창고'부터 만듭니다.

GitHub에 빈 저장소(Repository) 만들기

로컬(내 컴퓨터)에 Git 초기화하기: 프로젝트 폴더를 만들고 터미널에서 git init을 입력합니다.

기본 파일 만들기:

파이썬 코드를 작성할 main.py

Git에 올리지 않을 파일을 적어둘 .gitignore

프로젝트 설명서인 README.md

첫 번째 백업(Commit)과 업로드(Push):

git add . -> git commit -m "Docs: 프로젝트 초기 세팅 및 README 생성" -> git push

📍 Phase 2: 꺼지지 않는 메뉴판 만들기 (흐름 제어)
본격적인 파이썬 코딩입니다. 사용자가 번호를 고를 수 있는 껍데기를 만듭니다.

while True: 루프를 사용해 무한히 반복되는 메뉴 화면을 main.py에 출력합니다.

input()으로 숫자를 입력받고, if/elif/else로 1~5번 메뉴에 따라 안내문을 출력하게 합니다. (아직 퀴즈 기능은 없어도 됩니다. "퀴즈 풀기 선택됨" 정도만 출력하세요.)

예외 처리 방어막 치기:

문자(abc)를 입력하면? 프로그램이 꺼지지 않게 막아줍니다.

Ctrl+C를 누르면? 빨간 에러가 뜨지 않고 "안전하게 종료합니다"가 뜨도록 try/except KeyboardInterrupt를 사용해 봅니다.

Git Commit: git add main.py -> git commit -m "Feat: 기본 메뉴 화면 및 예외 처리 구현"

📍 Phase 3: 객체 지향의 시작 (설계도 그리기)
코드 공간을 깔끔하게 정리하기 위해 '클래스(Class)'라는 상자를 2개 만듭니다.

Quiz 클래스 만들기: 문제(question), 선택지 4개(choices), 정답(answer)을 묶어두는 틀을 만듭니다.

QuizGame 클래스 만들기: 게임 전체를 관리하는 틀입니다. 게임 안에 '퀴즈 목록'과 '최고 점수'를 변수로 가지게 합니다.

기본 퀴즈 5개 넣기: 내가 좋아하는 주제(예: 마블 영화, 파이썬 기초 등)로 Quiz 객체 5개를 직접 만들어 QuizGame에 넣어둡니다.

Git Commit: 기능별로 클래스를 만들었으니 커밋합니다. (Refactor: Quiz 및 QuizGame 클래스 구조화)

📍 Phase 4: 핵심 기능! 퀴즈 풀기 (브랜치 탐험)
여기서는 Git의 강력한 기능인 **브랜치(Branch, 평행우주)**를 사용해 봅니다. 메인 코드를 망치지 않고 새로운 기능을 개발하는 방법입니다.

브랜치 생성 및 이동: git checkout -b feature/play-quiz (이제 안전한 평행우주로 넘어왔습니다!)

퀴즈 풀기 기능 구현:

for문을 사용해 퀴즈 5개를 하나씩 화면에 보여줍니다.

사용자의 입력값과 정답을 비교해 맞으면 점수를 +1 해줍니다.

결과 화면을 보여줍니다.

병합(Merge):

기능이 잘 동작하면 커밋합니다. (Feat: 퀴즈 출제 및 정답 확인 기능 구현)

다시 메인 우주로 돌아옵니다. (git checkout main)

개발한 기능을 합칩니다. (git merge feature/play-quiz)

📍 Phase 5: 살 붙이기 (추가, 목록, 점수 기능)
나머지 메뉴들을 채워 넣습니다. (하나 완성할 때마다 꼭 커밋하세요!)

퀴즈 목록 보기: 현재 저장된 퀴즈들의 제목을 for문으로 쭉 출력합니다. (커밋: Feat: 퀴즈 목록 출력 기능 추가)

퀴즈 추가하기: input()으로 문제, 선택지, 정답 번호를 입력받아 Quiz 객체로 만든 뒤 목록에 추가합니다. (커밋: Feat: 새로운 퀴즈 등록 기능 추가)

점수 확인하기: 게임을 마쳤을 때 이전 최고 점수보다 높으면 점수를 갱신하고, 메뉴 4번을 누르면 그 점수를 보여줍니다. (커밋: Feat: 최고 점수 기록 및 확인 기능 추가)

📍 Phase 6: 생명력 불어넣기 (데이터 저장과 불러오기)
지금까지는 프로그램을 끄면 내가 추가한 퀴즈와 점수가 다 날아갑니다. 이를 state.json 파일에 영구 저장해 봅니다.

파이썬의 json 모듈을 가져옵니다. (import json)

저장하기(Write): 프로그램을 종료할 때 퀴즈 목록과 최고 점수를 state.json 파일로 만들어 저장합니다.

불러오기(Read): 프로그램이 처음 켜질 때 state.json 파일을 읽어서 퀴즈와 점수를 가져옵니다.

주의: 파일이 없거나 망가졌을 때 프로그램이 꺼지면 안 됩니다! try/except로 "파일이 없으면 기본 퀴즈 5개를 쓴다"라고 예외 처리를 해줍니다.

Git Commit: git commit -m "Feat: json 활용 데이터 저장 및 불러오기 기능 구현"

📍 Phase 7: 마무리와 협업 시뮬레이션 (Clone & Pull)
미션의 마지막 요구사항을 처리하며 완벽하게 끝냅니다.

README.md 작성: 프로젝트 설명, 실행 방법, 파일 구조 등을 상세히 적고 커밋, 그리고 GitHub에 git push 합니다.

협업 시뮬레이션 (아주 중요!):

내 컴퓨터의 전혀 다른 폴더로 이동합니다.

GitHub에서 내 저장소 주소를 복사해 git clone [내 저장소 주소]를 입력해 새로 다운받습니다.

새로 다운받은 폴더에서 README.md를 살짝 수정하고 다시 커밋/푸시합니다. (마치 다른 팀원이 코드를 수정한 것처럼!)

다시 원래 작업하던 폴더로 돌아와서 git pull을 쳐봅니다. 수정된 내용이 쏙 들어오는 것을 확인하면 완료입니다!


--------------------

1. 왜 하필 JSON인가요? (눈높이 설명)
**JSON(JavaScript Object Notation)**은 데이터를 저장하고 주고받는 데 가장 많이 쓰이는 '약속'입니다.

장점: 사람이 읽기 쉽고, 파이썬의 **딕셔너리(dict)**와 구조가 매우 비슷해서 변환하기 아주 편합니다.

UTF-8: 한글이 깨지지 않게 저장하기 위한 인코딩 방식입니다.

![alt text](image-1.png)

![alt text](image-2.png)

![alt text](image-3.png)


